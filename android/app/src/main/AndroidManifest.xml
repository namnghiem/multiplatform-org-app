<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trinitysmf.mysmf">

    <application
        android:name=".SmfApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.CardActivity"
            android:parentActivityName=".ui.activities.MainActivity"></activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden" android:launchMode="singleTask"
            />
        <activity android:name=".ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.LoginActivity" />
        <activity
            android:name=".ui.activities.ProfileEditActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activities.SectorActivity"
            android:parentActivityName=".ui.activities.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity android:name=".ui.activities.SectorEditActivity" />
        <activity android:name=".ui.activities.StockEditActivity" />
        <activity android:name=".ui.activities.MeetingEditActivity" />
        <activity android:name=".ui.activities.MeetingListActivity" />
        <activity android:name=".ui.activities.SectorListActivity" />
        <activity android:name=".ui.activities.SectorAddActivity" />
        <activity android:name=".ui.activities.MeetingAddActivity" />

        <activity android:name=".ui.activities.QrActivity" />
        <!--
     Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_trinity_logo11" />
        <!--
             Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

        <service android:name=".services.MessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

</manifest>